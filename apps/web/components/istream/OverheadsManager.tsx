"use client";
import {useState} from "react";import {useRouter} from "next/navigation";import {deleteBusinessOverhead,saveBusinessOverhead,type OverheadInput} from "@/lib/actions/financial-control.actions";import {OVERHEAD_CATEGORIES} from "./OverheadBudgetManager";
const blank=():OverheadInput=>({expenseDate:new Date().toISOString().slice(0,10),category:"Rent",payee:"",description:"",amount:0,paymentStatus:"PAID",paymentDate:new Date().toISOString().slice(0,10),reference:"",attachmentUrl:"",recurring:false,expenseType:"OPERATING"});
export default function OverheadsManager({rows}:{rows:any[]}){const router=useRouter();const [form,setForm]=useState<OverheadInput>(blank());const [error,setError]=useState("");const field={width:"100%",boxSizing:"border-box" as const,padding:9,border:"1px solid #475569",borderRadius:7,background:"#0f172a",color:"white"};async function save(){setError("");const r=await saveBusinessOverhead(form);if(!r.ok)return setError(r.error);setForm(blank());router.refresh()}function edit(x:any){setForm({...x,expenseDate:new Date(x.expenseDate).toISOString().slice(0,10),paymentDate:x.paymentDate?new Date(x.paymentDate).toISOString().slice(0,10):""})}async function remove(id:string){if(!confirm("Delete this overhead entry?"))return;await deleteBusinessOverhead(id);router.refresh()}const money=(v:number)=>new Intl.NumberFormat("en-US",{style:"currency",currency:"USD"}).format(v);return <><section><h3>{form.id?"Edit overhead":"Add overhead or capital purchase"}</h3><div className="grid"><div><label>Expense date</label><input type="date" style={field} value={form.expenseDate} onChange={e=>setForm({...form,expenseDate:e.target.value})}/></div><div><label>Type</label><select style={field} value={form.expenseType} onChange={e=>setForm({...form,expenseType:e.target.value})}><option value="OPERATING">Operating overhead</option><option value="CAPITAL">Capital expenditure</option></select></div><div><label>Category</label><select style={field} value={form.category} onChange={e=>setForm({...form,category:e.target.value})}>{OVERHEAD_CATEGORIES.map(x=><option key={x}>{x}</option>)}</select></div><div><label>Payee</label><input style={field} value={form.payee} onChange={e=>setForm({...form,payee:e.target.value})}/></div><div className="wide"><label>Description</label><input style={field} value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></div><div><label>Amount</label><input type="number" style={field} value={form.amount} onChange={e=>setForm({...form,amount:Number(e.target.value)})}/></div><div><label>Payment status</label><select style={field} value={form.paymentStatus} onChange={e=>setForm({...form,paymentStatus:e.target.value})}><option value="UNPAID">Unpaid</option><option value="PART_PAID">Part paid</option><option value="PAID">Paid</option></select></div><div><label>Payment date</label><input type="date" style={field} value={form.paymentDate||""} onChange={e=>setForm({...form,paymentDate:e.target.value})}/></div><div><label>Reference</label><input style={field} value={form.reference||""} onChange={e=>setForm({...form,reference:e.target.value})}/></div><div><label>Document link</label><input style={field} value={form.attachmentUrl||""} onChange={e=>setForm({...form,attachmentUrl:e.target.value})}/></div><label className="check"><input type="checkbox" checked={form.recurring} onChange={e=>setForm({...form,recurring:e.target.checked})}/> Recurring expense</label></div>{error&&<p className="error">{error}</p>}<button onClick={save}>{form.id?"Save changes":"Add overhead"}</button>{form.id&&<button className="secondary" onClick={()=>setForm(blank())}>Cancel</button>}</section><div className="table"><table><thead><tr>{["Date","Type","Category","Payee","Description","Amount","Payment","Recurring","Actions"].map(x=><th key={x}>{x}</th>)}</tr></thead><tbody>{rows.map(x=><tr key={x.id}><td>{new Date(x.expenseDate).toLocaleDateString("en-GB")}</td><td>{x.expenseType}</td><td>{x.category}</td><td>{x.payee}</td><td>{x.description}</td><td>{money(x.amount)}</td><td>{x.paymentStatus.replace("_"," ")}</td><td>{x.recurring?"Yes":"No"}</td><td><button className="small" onClick={()=>edit(x)}>Edit</button><button className="small danger" onClick={()=>remove(x.id)}>Delete</button></td></tr>)}</tbody></table></div><style jsx>{`section,.table{background:#1e293b;border:1px solid #334155;border-radius:12px;padding:18px;margin-bottom:18px}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:12px;margin-bottom:14px}.wide{grid-column:span 2}label{display:block;color:#cbd5e1;font-size:13px}.check{align-self:end}button{border:0;border-radius:7px;background:#2563eb;color:white;padding:10px 14px;font-weight:700;margin-right:7px}.secondary{background:#475569}.table{overflow-x:auto;padding:0}table{width:100%;min-width:1050px;border-collapse:collapse}th,td{text-align:left;padding:10px;border-bottom:1px solid #334155;font-size:13px}th{color:#cbd5e1}.small{padding:6px}.danger{background:#991b1b}.error{color:#fca5a5}@media(max-width:800px){.grid{grid-template-columns:1fr}.wide{grid-column:auto}}`}</style></>}
